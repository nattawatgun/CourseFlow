import deleteIcon from '../images/imagesCourse/delete.svg';
import editIcon from '../images/imagesCourse/edit.svg';

import courseFlow from '../images/imagesCourse/CourseFlow.svg';
import course from '../images/imagesCourse/course.svg';
import assignment from '../images/imagesCourse/assignment.svg';
import logout from '../images/imagesCourse/logout.svg';

import arrowDropDown from '../images/image/arrowDropDown.png';
import arrowBack from '../images/image/arrowBack.png';
import add from '../images/imagesAddCourse/add.svg';

import backIcon from '../images/imagesAddLesson/arrow_back.svg';


const imagesCourse = {
  deleteIcon, editIcon
};

const imagesSideBar = {
  courseFlow, course, assignment, logout
};

const imageMain = {
  courseFlow, course, assignment
};

const imageAssignment = {
  deleteIcon, editIcon
};

const imageAddAssignment = {
  arrowDropDown
};

const imageEditAssignment = {
  arrowDropDown, arrowBack
};


const addCourse = {
  add
};

const addLesson = {
  backIcon
};

export { imagesCourse, imagesSideBar, imageMain, imageAssignment, imageAddAssignment, imageEditAssignment, addCourse, addLesson };


